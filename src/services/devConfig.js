// TRUE = simular os dados que os endpoints retornam
// FALSE = utiliza as endpoints do backend
const config = {
    simularDados: false,
    //url: "http://localhost:8080"
    url: "https://total-wil-mette-sigos-6a0bb541.koyeb.app"
}

export default config
